// the basic types in vixen are
// bool, int, float, char, and string
func sum(x: int, y: int) -> int {
    return x + y;
}

func main() -> int {
    let isRunning: bool = true;

    loop (isRunning) {
        println("Hello, World!");
    }
}